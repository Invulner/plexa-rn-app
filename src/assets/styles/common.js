import { StyleSheet } from 'react-native'
import { NATIVE_GRAY } from './colors'

const commonStyles = StyleSheet.create({
  separatorLine: {
    backgroundColor: NATIVE_GRAY, 
    width: '100%', 
    height: 1
  }
})

export default commonStyles
