import { StyleSheet } from 'react-native'

const termsStyles = StyleSheet.create({
  title: {
    fontSize: 26,
    marginBottom: -10
  },

  text: {
    fontSize: 16
  }
})

export default termsStyles
